import styled from "styled-components";

const WatchedAndWatchlistStyle = styled.div`
  
  max-width: 840px;
  margin: auto;
    .top{
        padding: 2rem;
        .top-left{
            text-align: center;
        }
        .top-right{
            text-align: right;
        }


    }
    .main-section{
        padding: 1rem;
        box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
        margin-bottom: 3rem;
    }
`;

export default WatchedAndWatchlistStyle;
